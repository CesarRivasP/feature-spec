# Master Plan — changes[] dependency on a revised decision
## 1. Problema
`changes.C16` y `changes.C19` dependen de `decisions.export_location`, revisada
el 2026-09-10. `changes.C17` fue revisado después. `changes.C18` depende de
`decisions.kept_scope`, sin revisar.
