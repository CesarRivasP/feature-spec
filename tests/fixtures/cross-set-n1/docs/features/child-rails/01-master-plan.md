# Master Plan — child rails

## 1. Umbral heredado
El umbral sale de `docs/features/parent-tiles/_facts.yml limits.degradation_threshold_tiles`
y vale 2 tiles: es el numero contra el que se dimensiona C1.

El techo de frames sale de `docs/features/parent-tiles/_facts.yml limits.tolerated_dropped_frames`.
