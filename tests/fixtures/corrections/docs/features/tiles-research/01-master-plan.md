# Master Plan — research

## 1. Memoria
La purga no sirve: ver `limits.purge_frees_nothing`.

La primera lectura, `limits.purge_frees_nothing`, fue retractada el mismo día.

Lo que vale hoy es `limits.purge_frees_proportionally`, que reemplaza a `limits.purge_frees_nothing`.

## 2. Arnés
El arnés mide la versión fácil: `limits.same_url_is_easy`.

`limits.bare_retraction`, `limits.bare_correction`, `limits.dangling_retraction`, `defects.D1`, `defects.D2`.
