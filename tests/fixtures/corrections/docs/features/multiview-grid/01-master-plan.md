# Master Plan — grid

## 1. Premisas heredadas
La cache no ocupa CMA (`docs/features/tiles-research/_facts.yml limits.purge_frees_nothing`).

El arnés es la versión fácil (`docs/features/tiles-research/_facts.yml limits.same_url_is_easy`).

Lo que sigue vigente es `docs/features/tiles-research/_facts.yml limits.purge_frees_proportionally`.
