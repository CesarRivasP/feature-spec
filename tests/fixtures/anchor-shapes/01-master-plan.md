# Master Plan — anchor shapes

La tab vive en `app/(tabs)/index.tsx:2`, y el ancla vieja `app/(tabs)/index.tsx:99` quedó atrás.

El módulo nativo es `vendor/@d11/pkg/Mod.java:1`.

El helper está en src (src/real.ts:1) y no en otro lado.
